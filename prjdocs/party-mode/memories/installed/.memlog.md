---
topic: BMad Portal Hub
updated: 2026-08-06T19:32
---

- (dynamic) Winston et John s'opposent souvent en ouverture (archi robuste vs scope MVP) mais convergent vite quand le coût de recul est faible — Amelia arbitre côté implémentation, Sally ramène systématiquement au vécu utilisateur concret
- (outcome) BMad Portal Hub / auto-join HUB via repo distant : ID technique = chemin complet host/org/repo (unicité en base via contrainte, pas juste en UI) ; nom affiché = nom court ; statut d'accès à 3 valeurs (pending/active/access_revoked) porté par Amelia ; lien d'action scoped au provider unique du projet + fallback texte générique (John a retoqué la détection multi-provider comme prématurée) ; le zéro-config au premier lancement est un pari de design assumé par Nono, pas un besoin observé — gardé minimal tant que non validé
- (outcome) Etat repo local remonté par le Client Python: piggyback sur le heartbeat WebSocket AD-002 existant (pas de canal séparé), payload minimal par défaut + envoi immédiat déclenché par hook Git local (post-commit/post-checkout), tick 10s en filet de sécurité. Donnée brute = ahead/behind, working tree dirty/clean, branche, action en cours (rebase/merge/conflit). Cette donnée brute alimente FR5 (carte contributeur enrichie d'un badge, sans en faire un tableau Git complet) ET FR10/Epic4 (signaux de risque agrégés) - pas un 3e epic, une seule source, deux consommateurs. Nono a validé l'ensemble du découpage.
- (outcome) BMad Portal: converged on 'Fiche Contributeur' — a detailed per-contributor panel (access status, repo state incl. conflict/ahead-behind, linked projects, activity feed), not the glanceable 'Morning Pulse' digest first pitched. Nono wanted max info per contributor, not a summary row. Built entirely on existing data (access enum + repo heartbeat), no new pipes — Winston conceded fast once framed as a read model. Sally shipped an HTML mock in the existing Modern Command navy identity.
- (outcome) Readiness report flagged no scaffolding story for the 3-tier greenfield stack. John resisted calling it a 'user story' (no user value, no FR); Winston/Amelia pushed back on invisibility risk. Landed on Epic 0 / Sprint 0, Story 0.1 — enabler, no FR mapping, scope = 3-tier local scaffolding + initial PG migrations + CI (lint+test only, no deploy topology, which stays deferred per spine). Applied to epics.md and ARCHITECTURE-SPINE.md Capability Map same session.
- (moment) Session closed 2026-08-06. Tight, fast-converging session — John's 'no fake user value' objection and Winston's 'invisible work' worry resolved in a handful of turns once reframed as an enabler story. Amelia drafted the AC live in-room; Mary insisted the FR Coverage Map say 'no FR' explicitly rather than omit Epic 0 silently — recurring pattern for this room: unexplained absence reads as an oversight, an explained absence doesn't.
